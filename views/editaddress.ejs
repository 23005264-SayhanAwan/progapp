<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Healthy Mealmaker - Update Address</title>
    <%- include('partials/navbar.ejs') %>
</head>
<body>
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Address</h2></div>
        <br>
        <div class="text-center">
            <% if (address) { %> 
                <form action="/editaddress/<%= address.delivery_id %>" method="POST">
                    <label for="unit_number">Unit Number:</label><br>
                    <input type="text" id="unit_number" name="unit_number" value="<%= address.unit_number %>" required><br><br>

                    <label for="postalcode">Postal Code:</label><br>
                    <input type="text" id="postalcode" name="postalcode" value="<%= address.postalcode %>" required><br><br>

                    <button type="submit" class="btn btn-primary">Update Address</button>
                </form>
            <% } else { %>
                <p>No Address found.</p>
            <% } %>
            <a href="/viewaddresses">Back</a>
        </div>
    </div>
</body>
</html>
